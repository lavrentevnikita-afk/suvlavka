# Руководство по вкладу в проект (CONTRIBUTING)

Спасибо, что хотите помочь развитию **Souvenir Shop**!  
Этот документ описывает, как предлагать изменения и работать с кодовой базой.

---

## 1. Как предложить изменения

1. **Создайте issue** (если его ещё нет):
   - кратко опишите проблему или фичу;
   - при необходимости приложите скриншоты или схемы.

2. **Создайте ветку** от `main`:
   - `feature/<кратко>` для новых возможностей;
   - `fix/<кратко>` для исправлений;
   - `chore/<кратко>` для инфраструктурных задач.

3. **Сделайте изменения**:
   - следуйте рекомендациям из `DEVELOPMENT.md`;
   - придерживайтесь принятого стиля кода.

4. **Откройте pull request**:
   - опишите, что сделано и почему;
   - укажите, есть ли миграции/изменения схемы БД;
   - добавьте скриншоты, если меняется UI.

---

## 2. Стиль кода и форматирование

### 2.1. Frontend (Nuxt/Vue/TS)

- Используем `<script setup lang="ts">`.
- Компоненты — в PascalCase, composables — `useSomething`.
- TailwindCSS-классы — по общей дизайн-системе (не изобретать свои размеры от балды).
- Инструменты (по мере настройки):
  - ESLint + Prettier;
  - Stylelint для стилей (если понадобится).

Перед коммитом желательно:

```bash
cd frontend
npm run lint
```

*(Команда может быть заглушкой на ранних этапах.)*

### 2.2. Backend (NestJS/TS)

- Стиль — правило `@typescript-eslint` + Prettier.
- Структура модулей:
  - `Module` → `Controller` → `Service` → `Repository`.
- Все публичные методы в контроллерах и сервисах должны быть типизированы.

---

## 3. Коммиты

Рекомендуется использовать **Conventional Commits**:

- `feat: ...` — новая функциональность;
- `fix: ...` — исправление;
- `docs: ...` — документация;
- `chore: ...` — инфраструктура, зависимости;
- `refactor: ...` — изменение внутренней логики без изменения поведения.

Примеры:

- `feat: add b2b quick order table`
- `fix: correct price calculation for store role`
- `docs: update architecture for nestjs`
- `chore: add docker-compose for dev`

---

## 4. Тесты

- Frontend:
  - Vitest — модульные тесты компонентов/composables;
  - Playwright — e2e-сценарии (каталог → товар → корзина → заказ).
- Backend:
  - Jest (стандарт для NestJS) — тесты контроллеров и сервисов.

По возможности перед PR:

```bash
# Frontend
cd frontend
npm test           # или npm run test:unit / test:e2e по мере настройки

# Backend
cd backend
npm test           # Jest
```

---

## 5. Code review

Каждый PR проходит ревью:

- архитектура и читаемость;
- влияние на производительность и UX;
- корректность работы с БД и транзакциями;
- покрытие тестами ключевых участков.

Ревьюер может попросить:

- разбить PR на более мелкие;
- добавить тесты;
- упростить/декомпозировать логику.

---

## 6. Документация

Если вы:

- добавляете новые API,
- меняете структуры сущностей,
- существенно влияете на UX,

то **обновите**:

- `ARCHITECTURE_AND_DEPLOYMENT.md`,
- `DEVELOPMENT.md`,
- `ROADMAP.md`,
- и по необходимости `README.md`.

---

## 7. Поведение

Мы придерживаемся уважительного и конструктивного общения:

- обсуждаем решения, а не личности;
- даём предметную обратную связь;
- помним, что цель — общий качественный продукт.

Спасибо за ваш вклад! 🙌
